import styled from '@emotion/styled';

export const CommentsStyle = styled.aside`
  background-color: #fff;
  display: block;

  > div {
    margin-left: auto;
    margin-right: auto;
    max-width: 1024px;
    padding: 2rem 1.2rem;
  }

  h3 {
    font-size: 1.4rem;
    font-weight: 700;
    margin: 0 0 1.2rem;

    small {
      opacity: 0.75;
    }
  }
`;
